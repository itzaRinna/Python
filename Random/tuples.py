#A tuple is immutable, meaning you just cannot change it even if you want to
coordinates = (4, 5)

print(coordinates[0])
#You can modify a list, but cannot change a tuple
#you can make a list of tuples
coordinates_1 =[(4, 5), (6, 7), (80, 40)]
print(coordinates_1)
